import {createTheme} from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: '#A1C4FD',
        },
        secondary: {
            main: '#A67458',
        },
    },
});

export default theme;